1. git init

// to check version
2. git --version 

// adding origin 
3. git remote add origin <Git repo url | SSH | HTTPs key>
4. git pull origin master
5. git add .

// to check if repo is fine
6. git status

7. git commit -m "your message here"
8. git push origin master

// don't forget to rename branch as 'master'
